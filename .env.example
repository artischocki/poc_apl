# --- LLM ---
# OpenAI (default)
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
OPENAI_API_KEY=sk-...

# Anthropic (switch when ready)
# LLM_PROVIDER=anthropic
# LLM_MODEL=claude-sonnet-4-6
# ANTHROPIC_API_KEY=...

# --- Frontend (local dev only; Docker overrides these via docker-compose.yml) ---
BACKEND_URL=http://localhost:8000
CHAINLIT_DB_URL=sqlite+aiosqlite:///./chainlit.db
CHAINLIT_USERNAME=admin
CHAINLIT_PASSWORD=admin
CHAINLIT_AUTH_SECRET=changeme-use-a-long-random-string
